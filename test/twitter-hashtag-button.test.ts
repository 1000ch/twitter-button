import { TwitterHashtagButton } from '../src/twitter-hashtag-button';

describe('TwitterHashtagButton', (): void => {
  test('should be defined', (): void => {
    expect(TwitterHashtagButton).toBeDefined();
  });
});
