import { TwitterMentionButton } from '../src/twitter-mention-button';

describe('TwitterMentionButton', (): void => {
  test('should be defined', (): void => {
    expect(TwitterMentionButton).toBeDefined();
  });
});
