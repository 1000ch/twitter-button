import { TwitterFollowButton } from '../src/twitter-follow-button';

describe('TwitterFollowButton', (): void => {
  test('should be defined', (): void => {
    expect(TwitterFollowButton).toBeDefined();
  });
});
